[package]
name = "bilibili-box-plugin-sdk"
version = "0.1.0"
description = "BiliBox Plugin SDK"
authors = ["BiliBox"]
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
